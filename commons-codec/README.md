# yokra9-commons-codec

`moditect-maven-plugin` を利用し、[commons-codec](https://mvnrepository.com/artifact/commons-codec/commons-codec) に `module-info.class` を追加して再パッケージングしたもの。

```powershell
# コンパイルとインストール
mvn clean install
```
